/** Options for {@link import('./daytona/index').DaytonaProvider} — kept in a separate file so the main package entry stays free of `@daytonaio/sdk`. */
export interface DaytonaProviderOptions {
    apiKey?: string;
    sandboxId?: string;
}
